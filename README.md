# VLSM-Calculator-Python
Calculator that when given Network Address/Mask and Hostcount inputs, will generate VSLMs in order of largest to least. It also outputs the last usable host address as a gateway address along with the broadcast address for that specific subnet.
